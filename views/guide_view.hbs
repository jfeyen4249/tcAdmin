
<!DOCTYPE html>
<html lang="en">
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <style>
        body {
            background: linear-gradient(to bottom, #000, #0f9b0f);
            display: flex;
            justify-content: center;
            align-items: flex-start;
            background-repeat: no-repeat;
            background-position: fixed;
            background-attachment: fixed;
            height: 95vh;
            margin: 0;
            padding-top: 2rem;
            font-size: 24px;
        }
        .content {
            background-color: white;
            width: 70vw;

            height: calc(95vh - 2rem);
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.449);
            border-radius: 8px;
            text-align: center;
            overflow-y: auto;
        }

        img {
            max-width: 65vw;
            height: auto;
        }

        .tc {
            font-weight: 100;
        }

        .guide {
            font-weight: 800;
            font-size: 32px;
            font-style: italic;
            
        }

        .ql-container {
            height: 100%; /* Fill the content div */
        }
    </style>
</head>
<body>
    <div class="content">
        <h1>{{title}}</h1>
        <hr>
        <div id="editor-container">
            {{{description}}}
        </div>
    </div>
</body>
</html>
